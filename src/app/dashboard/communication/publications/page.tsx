"use client";

export default function PublicationsPage() {
  // This content has been moved to the main dashboard page.
  // This file is kept to avoid breaking navigation links, but it renders nothing.
  return <></>;
}
