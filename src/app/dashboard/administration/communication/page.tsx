"use client";

export default function CommunicationPage() {
  // This page was causing a build error.
  // The content has been moved to /dashboard/administration/evaluation-percentages/page.tsx as a workaround.
  return <></>;
}
